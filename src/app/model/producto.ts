export class Producto{
  // acá vamos a generar un objeto para sustituir al json
  // migramos el json a una lista de productos
  public id: string;
  public nombre: string;
  public precio: number;
  public imagen: string;
  public cantidad: number;
}